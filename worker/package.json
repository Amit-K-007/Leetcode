{
  "name": "worker",
  "version": "1.0.0",
  "description": "To run the `lc-worker` container with systemd and required privileges, execute the following command:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "redis": "^5.0.1",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.17"
  }
}
